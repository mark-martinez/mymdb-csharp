<UserControl x:Class="MyMDB.Core.View.Results"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             VerticalAlignment="Stretch">

    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
            <Button Content="Return" Command="{Binding ReturnToLanding}"/>
            <Button Content="List" Command="{Binding DatabaseContent}"/>
        </StackPanel>
        <DockPanel>
            <ListView Name="itemList"  ItemsSource="{Binding ResultsList}" HorizontalContentAlignment="Stretch">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <i:InvokeCommandAction Command="{Binding DataContext.ResultClickHandler,
                            RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                               CommandParameter="{Binding}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ListView>
        </DockPanel>
    </DockPanel>
</UserControl>
